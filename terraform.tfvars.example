# Set exactly one: project_id or org_id.

project_id = "acme-prod"

# org_id          = "123456789012"
# billing_account = "01ABCD-2345EF-6789GH"

collector_service_account = "log360-collector@acme-prod.iam.gserviceaccount.com"

sources = [
  "audit_activity",
  "audit_system_event",
  "audit_policy",
  "firewall",
  "nat",
  "load_balancer",
  # "audit_data_access",   # often larger than everything else combined
]

# resource_scoping = {
#   nat = {
#     label  = "resource.labels.gateway_name"
#     values = ["nat-gw-use1"]
#   }
# }
